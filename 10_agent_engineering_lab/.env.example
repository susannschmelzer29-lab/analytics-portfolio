# Copy to .env and fill in. .env is gitignored and must NEVER be committed.
#
# A key that has already been pushed counts as compromised: rotate it,
# do not delete it. It does not disappear from git history on its own.

ANTHROPIC_API_KEY=sk-ant-put-it-here
